use todo;
-- create table login (Username varchar(20), EmailID varchar(20), Pass varchar(20));
-- create table todos (title varchar(200), details varchar(500), edit_time timestamp);
SHOW TABLES;
select * from param;
-- DROP TABLE param;